# Starting prompt for Claude Code or Codex

Use the following prompt when Will is ready to request implementation. Creating this handoff has not started or authorized a production build. Supply the extracted package and the existing repository to the implementing agent.

---

Implement the Architecture practice design for WillGodfrey.com using the attached handoff package and the existing website repository. Build a complete private/local implementation preview for my review. Do not publish, merge, change DNS or change hosting settings.

First read `README.md`, all six documents in `docs/`, `content/site-content.json`, `design-tokens.json`, and `assets/asset-manifest.json`. Open all six mockups in a browser at desktop and phone sizes; inspecting HTML source alone is not a visual review.

Use this package as the selected design brief. The three earlier concept options are no longer alternatives to explore. Match the Architecture practice composition, material artwork, colors, typography, image crops, spacing and purposeful interactions. Do not redesign it into a generic template.

The package includes proposed copy. Use that exact copy to make a reviewable implementation preview and report its material differences from the existing copy. This instruction authorizes applying the proposed copy in that preview; final editorial approval is required before launch. Keep all text in the repository's canonical content source, including metadata, alt text, captions and interface labels. Do not invent claims, client names, metrics, dates, workflow details, photography or testimonials.

Inspect the existing repository and its instructions before editing. It was already an Astro static site when this handoff was prepared; adapt that app with its pinned toolchain and lockfile. Recheck the actual baseline instead of assuming it is unchanged. Preserve unrelated user changes. Resolve historical design conflicts explicitly using the handoff's decision record. Keep documented operational decisions such as the `www` canonical host, generic method/platform wording and approved analytics behavior.

Implement all five routes and the custom 404, real page navigation, full Work cases and anchors, the complete definition and visible FAQ, and the specified Home work/stage and Practice layer interactions. Render all content in HTML first, with readable no-JavaScript fallback. The Practice engagement sequence is static; the three specified selectors are small independent enhancements, not a reason to install a UI framework.

Use the bundled artwork, diagrams, fonts and licenses. The existing small portrait and labeled conceptual drawings are complete fallbacks, so optional new photography and undisclosed client details must not block implementation. Keep the sculpture source master out of routine production transfer and use the responsive derivatives. Preserve the existing share-image path unless I request otherwise.

Treat the mockup files as visual and behavioral references. Do not ship the review gallery, copy its prototype markup wholesale, leave review labels in public output, or claim the prototype already satisfies production metadata, accessibility, security or performance requirements.

Follow the implementation plan and acceptance checklist. Validate copy, schemas, routes, anchors, images, all selector states, keyboard/focus behavior, reduced motion, no-JavaScript reading, mobile reflow, print and actual production-build performance. Compare matched desktop and phone captures against the supplied mockups. Run meaningful checks, fix failures, and report actual results without claiming checks that were not run.

Use reasonable documented defaults for reversible implementation choices. Complete the reviewable preview, then give me its location, desktop/phone screenshots, test results, editorial change list and any remaining deviations. Ask only about information that genuinely prevents further work. Keep launch as a separate decision after I can inspect the finished preview.

---

## Where to begin reading

The PRD describes the product; the page specs describe what must appear; the design system and mockups show how it should look; the content manifest gives exact text; the implementation plan maps everything into the existing repo. The acceptance checklist defines done.
